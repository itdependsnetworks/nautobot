Fixed per-row database queries in several Extras list views: contact/team associations (contact and team are now joined in), object metadata (metadata type and contact/team joined in), relationship associations (source/destination generic foreign keys prefetched when no stale content types are present), scheduled jobs (approval workflows prefetched for the approval-state column), and dynamic group tables (group and member content type joined in, including the nested ancestors/descendants tables).
