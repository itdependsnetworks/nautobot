Added a `columns` argument to `BaseTable`, an explicit list of column names to show that takes precedence over SavedView table configuration and the user's saved preferences. Useful for programmatic table construction, and used by tooling that audits per-column query behavior (column visibility drives the table's conditional prefetches, so it must be controlled at construction time).
