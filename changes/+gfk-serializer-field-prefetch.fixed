Fixed N+1 queries in REST API list responses for models with `GenericForeignKey` fields (e.g. Note, ObjectMetadata, StaticGroupAssociation, ContactAssociation, RelationshipAssociation) — the automatic queryset optimization now prefetches all `GenericForeignKey` fields on the model, whether rendered as a serializer field or read through `display`, covering app-defined models as well.
