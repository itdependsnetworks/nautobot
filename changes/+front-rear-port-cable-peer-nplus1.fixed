Fixed an N+1 query in the Front Ports and Rear Ports REST API list endpoints — the `cable_peer` field issued a cable-termination lookup for every port in the response. The viewsets now apply the same cable-peer prefetch already used by the Interfaces and Console Ports endpoints.
