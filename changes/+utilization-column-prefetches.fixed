Reduced per-row queries in list-view utilization columns: the Racks list "Space" column now prefetches each rack's positioned devices and reservations instead of querying per rack, and the Prefixes list utilization column now prefetches each prefix's count-as-utilized IP address ranges. (The per-prefix contained-IP scan and the power-utilization aggregates remain per-row computations.)
